services:
  - type: web
    runtime: python
    name: tasksim-lab
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/server.py --host 0.0.0.0 --model models/alibaba_xgboost_100k.json --train-on-start
    healthCheckPath: /api/health
